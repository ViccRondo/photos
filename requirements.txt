# Python 依赖
# 运行 install.sh 会自动安装：
# 1) apt 系统包：python3-opencv、python3-pil
# 2) venv pip 包：RPi.GPIO、spidev、gpiozero

# 核心依赖
gpiozero>=2.0
spidev>=3.5
RPi.GPIO>=0.7

# OpenCV / Pillow 建议使用系统包安装，避免树莓派上反复下载大文件 wheel：
# sudo apt install -y python3-opencv python3-pil
